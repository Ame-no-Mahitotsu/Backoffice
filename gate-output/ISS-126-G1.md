---
ticket: ISS-126
gate: G1
agent: Claire (Automated)
date: 2026-03-21
---

## Plan

### Branch
`feature/iss-126-clickable-ticket-rows` from `main`

### Files to change

1. **`templates/tickets.html`**
   - Target: the `<tr>` inside the `{% for t in tickets %}` loop (currently line ~84)
   - Current: `<tr style="border-bottom:1px solid #eee;">`
   - Change: add `onclick="window.location='/tickets/{{ t['id'] }}'"` attribute and append `cursor:pointer;` to the inline style
   - Result: `<tr style="border-bottom:1px solid #eee; cursor:pointer;" onclick="window.location='/tickets/{{ t[\'id\'] }}'">`
   - The existing `<a href="/tickets/{{ t['id'] }}">View</a>` anchor in the last `<td>` is **not touched** — AC2 is satisfied structurally, no code change needed there.

2. **`tools/tests/test_dashboard.py`**
   - Add three new test functions at the end of the file, all using the `seeded_client` fixture:
     - `test_ticket_row_has_onclick_navigating_to_detail_view` — verifies `onclick="window.location='/tickets/ISS-001'"` appears in rendered `/tickets` HTML (AC1, AC4)
     - `test_ticket_row_has_cursor_pointer_style` — verifies `cursor:pointer` appears in the rendered `<tr>` element (AC3, AC4)
     - `test_ticket_row_view_link_still_present` — verifies `href="/tickets/ISS-001"` still appears in rendered `/tickets` HTML (AC2, AC4)

### Test plan

| Test name | What it verifies | AC covered |
|---|---|---|
| `test_ticket_row_has_onclick_navigating_to_detail_view` | GET `/tickets` renders a `<tr>` with `onclick="window.location='/tickets/ISS-001'"` | AC1, AC4 |
| `test_ticket_row_has_cursor_pointer_style` | GET `/tickets` renders a `<tr>` containing `cursor:pointer` in its style attribute | AC3, AC4 |
| `test_ticket_row_view_link_still_present` | GET `/tickets` still contains `href="/tickets/ISS-001"` (View anchor untouched) | AC2, AC4 |

All three tests use `seeded_client` (which seeds ISS-001, ISS-002, ISS-003) and check `response.data.decode("utf-8")` — consistent with existing test patterns in the file.

### AC coverage

| AC | How covered |
|---|---|
| AC1: clicking a row navigates to /tickets/<id> | `onclick="window.location='/tickets/{{ t['id'] }}'"` added to each `<tr>` in loop; verified by `test_ticket_row_has_onclick_navigating_to_detail_view` |
| AC2: View button still works | View anchor `<a href="/tickets/{{ t['id'] }}">` is untouched; clicking it navigates independently (anchor default fires before row onclick bubbles — same destination, no conflict); verified by `test_ticket_row_view_link_still_present` |
| AC3: cursor:pointer on hover | `cursor:pointer` appended to `<tr>` inline style; verified by `test_ticket_row_has_cursor_pointer_style` |
| AC4: at least 1 test | All three tests above satisfy this; any one would suffice |

### Notes

- **Event bubbling (AC2 safety):** Clicking the View anchor fires anchor navigation first; the `onclick` on the parent `<tr>` receives the bubbled event but the page has already begun navigating to the same URL. No visible UX issue. `event.stopPropagation()` on the anchor is not required by any AC and is intentionally omitted to keep changes minimal.
- **No JS framework dependency:** `window.location` assignment is plain JS — no library needed, no `dashboard.py` change required.
- **`dashboard.py` not touched:** The `/tickets/<ticket_id>` route is already live (ISS-097). This is a template-only change.
- **`conftest.py` not touched:** `seeded_client` fixture already exists in `test_dashboard.py` and is sufficient.
- **Scope:** All changes are within allowed paths (`templates/tickets.html`, `tools/tests/`). No cross-cutting changes detected.
