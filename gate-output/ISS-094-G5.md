---
ticket: ISS-094
gate: G5
agent: Alessandro
date: 2026-03-20
branch: feature/iss-094-note-length-cap
---

## G5 — Tech Lead Review: APPROVED

### What I reviewed

- `ticket.py` diff — `_MAX_NOTE_LEN` constant + guards in `handle_create` and `handle_update`
- `tools/tests/test_ticket_note_length.py` — new test file, 4 tests
- Full test suite: 186 passed

### Findings

**Constant placement is correct.**
`_MAX_NOTE_LEN = 2000` defined at module level after `_CLOSE_RESOLUTIONS`. Right location — visible to all handlers, no magic numbers inline.

**Guard logic is correct.**
`if args.notes and len(args.notes) > _MAX_NOTE_LEN` — the truthy check before `len()` means an empty/None notes value is never rejected. Boundary at 2000 (inclusive) is correct: 2001 raises, 2000 does not. AC3 ✓

**`handle_close` is covered.**
`handle_close` constructs a namespace and delegates to `handle_update` — the cap therefore applies to `close --note` without any additional code. ✓

**Advisory (not a blocker):** `handle_reopen` has its own note-writing path and does not call `handle_update`. The 2000-char cap is not applied there. This is outside AC scope (AC2 specifies `create` and `update` only) but worth a follow-up ticket if Arale routing ever calls `reopen`.

**Tests are well-structured.**
`_create_args` and `_update_args` helper factories keep the test bodies clean. Boundary cases (2000 = accept, 2001 = raise) correctly map to AC3. No over-testing.

**Standards compliance.**
- Branch name: `feature/iss-094-note-length-cap` ✓
- TDD: red at G2 (2 failures), green at G3 ✓
- No unrelated changes ✓

### Verdict

**APPROVED.** Route to G6 (Chris).
