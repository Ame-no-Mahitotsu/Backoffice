# ISS-097 — Gate G3 (Implementation Green)

## Branch
`feature/iss-097-ticket-detail-view` — commit `7003675`

## Changes applied

### `dashboard.py`
- Added `abort` to Flask imports.
- New route `GET /tickets/<ticket_id>`:
  - Queries DB for single ticket by ID; `abort(404)` if not found.
  - Passes `back_url = request.referrer or "/tickets"` to template for Back link.
  - Renders `ticket_detail.html`.

### `templates/ticket_detail.html` (new file)
- Extends `base.html`. Shows all ticket fields in a styled `<table>`.
- Fields displayed: id, title, type, status, urgency, severity, sprint, owner,
  component, tags, blocks, blocked-by, date, updated, closed, project, notes.
- Back link: `<a href="{{ back_url }}">← Back</a>`.

### `templates/tickets.html`
- Added a 10th `<th>` column (empty header) and a "View" link cell per row:
  `<a href="/tickets/{{ t['id'] }}">View</a>`

### `tools/tests/test_dashboard.py`
- 4 new tests added:
  - `test_ticket_detail_returns_200_for_existing_ticket` (happy path — AC1, AC5)
  - `test_ticket_detail_returns_404_for_missing_ticket` (404 guard — AC4, AC5)
  - `test_ticket_detail_shows_all_required_fields` (content check — AC2)
  - `test_tickets_list_has_view_link` (View link — AC1)

## Post-implementation test run — 40 tests GREEN

```
platform win32 -- Python 3.14.3, pytest-9.0.2
rootdir: C:\temp\ClaudeProjects\SitoPresepi2

30 dashboard tests PASSED (26 pre-existing + 4 new ISS-097)
10 sprint tests PASSED (8 pre-existing + 2 ISS-096)

40 passed in 1.48s
```

## Coverage
`dashboard.py` — 93% (up from 92% with ISS-096; new route fully covered)

## Acceptance criteria verification
- AC1 ✅ "View" link in ticket list → `GET /tickets/<id>` → detail page
- AC2 ✅ All 17 ticket fields (id, title, type, status, urgency, severity, sprint,
         owner, component, tags, blocks, blocked-by, date, updated, closed, project, notes)
         rendered in `ticket_detail.html`
- AC3 ✅ `back_url = request.referrer or "/tickets"` — preserves Referer when available
- AC4 ✅ `abort(404)` for missing ticket ID; test confirmed
- AC5 ✅ 4 new tests (≥ 2 required); all 36 pre-existing tests pass unchanged
