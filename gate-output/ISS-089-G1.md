---
ticket: ISS-089
gate: G1
agent: Alessandro
date: 2026-03-20
branch: feature/iss-089-formalise-gate-note-syntax
---

## Task interpretation

Formalise `[GATE N]` (square brackets, word GATE, space, gate number) as the one and only valid format for gate markers in ticket notes. The `ticket.py list --gate` filter depends on this exact pattern via substring match. The incident: ISS-078 used `G5:` prefix — the filter missed it. Document this as a hard rule in ai-standards.md.

## Files to be touched

- `memory/ai-standards.md` — add explicit rule under the "Ticket notes" section of the Agent Execution Protocol

## Implementation

Add a boxed rule immediately after the existing format line:
> **Canonical format is `[GATE N]` — no variations permitted.** Alternatives such as `G5:`, `GATE 5:`, or `[G5]` are invalid. The `--gate` filter in `ticket.py list` performs a substring match on `[GATE N]` markers; any deviation silently breaks gate-queue visibility.

## G2

N/A — documentary change only. No code modified, no tests needed.

## Reviewer assignment

Per ai-standards.md §Reviewer Assignment — When Alessandro is the Implementer:
- G5: Patrick (process/architecture — this touches ai-standards.md and gate protocol)
- G6: Athena

## Risks

None. Additive documentation only.
