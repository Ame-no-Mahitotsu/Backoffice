---
ticket: ISS-085
gate: G2
agent: Alessandro
date: 2026-03-20
branch: feature/iss-085-sqlite-reader-empty-list-guard
---

## Test written

`test_query_tickets_empty_list_filter_returns_all_rows` in `tools/tests/test_sqlite_reader.py`

## pytest output (red)

```
FAILED tools/tests/test_sqlite_reader.py::test_query_tickets_empty_list_filter_returns_all_rows
AssertionError: assert 0 == 1
 +  where 0 = len([])
```

Note: `TicketQuery(status=[])` emits `status IN ()` → SQLite returns 0 rows (no error in this Python version, but 0 rows is wrong — the filter should be ignored).
