# ISS-095 — Gate G1 (Analysis & Plan)

## Ticket interpretation
ISS-095 is a pure refactor task in `tools/tests/test_dashboard.py` to move inline imports from function scope to module scope, with zero behavioral change.

Acceptance criteria mapping:
- **AC1**: Move all inline `import tempfile` and `from pathlib import Path as _Path` imports to module level.
- **AC2**: Preserve behavior; confirm with baseline and post-change green test runs.
- **AC3**: Modify no files other than `tools/tests/test_dashboard.py`.

## Inline imports identified
1. `test_tickets_view_filter_date_from`
   - `from pathlib import Path as _Path`
   - `import tempfile`
2. `test_tickets_view_filter_date_to`
   - `import tempfile`
   - `from pathlib import Path as _Path`
3. `test_tickets_view_keyword_search_matches_tags`
   - `import tempfile`
   - `from pathlib import Path as _Path`
4. `test_tickets_view_keyword_search_does_not_match_when_no_tag_title_notes`
   - `import tempfile`
   - `from pathlib import Path as _Path`

## Refactor plan
- Add `import tempfile` once at module level near existing imports.
- Keep existing module-level `from pathlib import Path` import.
- Remove all inline `from pathlib import Path as _Path` imports.
- Replace `_Path(...)` usages with `Path(...)` in affected functions.
- Remove all inline `import tempfile` statements.

## Files to be touched
- `tools/tests/test_dashboard.py` (only)
- `memory/workspace/tooling/gate-output/ISS-095-G1.md` (gate report artifact)

## Risks and mitigations
- **Risk**: Accidentally altering test behavior while refactoring imports.
  - **Mitigation**: baseline green run before changes (G2), then full rerun after changes (G3).
- **Risk**: Violating AC3 by touching extra files.
  - **Mitigation**: restrict edits to `tools/tests/test_dashboard.py` only.

## Branching note
Branch created: `feature/iss-095-test-dashboard-inline-imports` from `main`.

## G2 strategy note
This is a pure refactoring ticket. G2 is a **baseline green confirmation** using existing tests (no new tests authored), and those tests serve as the safety net for behavior preservation.
