# ISS-098 — Gate G2 (Red Test)

## Branch
`feature/iss-098-sticky-nav` from `main` @ 0c63163

## New test added
`tools/tests/test_dashboard.py::test_nav_bar_has_sticky_positioning`

Asserts that the rendered HTML for `GET /` contains `position:sticky`,
`position: sticky`, `position:fixed`, or `position: fixed` in the CSS.

## Red test run

```
platform win32 -- Python 3.14.3, pytest-9.0.2
rootdir: C:\temp\ClaudeProjects\SitoPresepi2

tools/tests/test_dashboard.py::test_nav_bar_has_sticky_positioning FAILED

FAILURES:
  test_nav_bar_has_sticky_positioning
    assert ('position:sticky' in body or 'position: sticky' in body or
            'position:fixed' in body or 'position: fixed' in body)
    AssertionError: current base.html has no sticky or fixed positioning CSS

1 failed in 0.50s
```

## Why this is the correct red failure
The current `templates/base.html` `nav` CSS is:
```css
nav { background: #16213e; display: flex; gap: 0; }
```
No `position:sticky` / `position:fixed` property — hence the assertion fails.
After adding sticky CSS to `base.html`, this test will go green.

## Ready to implement
Proceeding to G3: add `.sticky-top { position:sticky; top:0; z-index:100; }`
wrapper in `templates/base.html` around header + nav elements.
