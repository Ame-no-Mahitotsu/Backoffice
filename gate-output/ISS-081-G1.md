---
ticket: ISS-081
gate: G1
agent: Alessandro
date: 2026-03-20
branch: feature/iss-081-dashboard-polish
---

## Task interpretation

Cross-view polish pass on the PM Dashboard. Blocking tickets ISS-078/079/080 are all resolved.

Audit of current state:
- **Active tab highlight** — already implemented in base.html + all routes pass `active=`. No change needed.
- **Empty state messages** — present in all three templates. No change needed.
- **Page titles** — `<title>PM Dashboard</title>` is hardcoded in base.html. All views show the same title. Fix: add `{% block page_title %}` block so each view can set its own title.
- **Consistent styling** — tickets.html uses `font-size:0.88rem` on the table; messages.html uses `font-size:0.9rem`. Normalise to `0.9rem`.

## Files to be touched

- `templates/base.html` — add `{% block page_title %}PM Dashboard{% endblock %}` inside `<title>`
- `templates/tickets.html` — add `{% block page_title %}Tickets | PM Dashboard{% endblock %}` + fix table font-size
- `templates/messages.html` — add `{% block page_title %}Messages | PM Dashboard{% endblock %}`
- `templates/sprint.html` — add `{% block page_title %}Sprint | PM Dashboard{% endblock %}`
- `tools/tests/test_dashboard.py` — add 3 page-title tests

## Tests to write at G2

- `test_tickets_page_title` — GET /tickets → `<title>Tickets | PM Dashboard</title>`
- `test_messages_page_title` — GET /messages → `<title>Messages | PM Dashboard</title>`
- `test_sprint_page_title` — GET /sprint → `<title>Sprint | PM Dashboard</title>`

## Reviewer assignment

G5: Chris (code changes)
G6: Athena

## Risks

None — template-only changes. No route logic altered.
