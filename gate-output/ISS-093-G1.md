---
ticket: ISS-093
gate: G1
agent: Dominick
date: 2026-03-19
branch: feature/iss-093-secrets-scanner-recommendation
---

## Task interpretation

Evaluate `detect-secrets` (Yelp) vs `gitleaks` (zricethezav) for use as a pre-commit secrets scanner in this project. Produce a recommendation document (tool choice, pinned version, baseline generation steps, `.pre-commit-config.yaml` snippet, and project-specific exclusion guidance) that Salvatore can implement directly in ISS-090.

This is a documentation-only ticket. No production code is changed. The deliverable is a committed markdown file and detailed notes that unblock ISS-090.

## Existing environment (relevant to this ticket)

- No `.pre-commit-config.yaml` exists — ISS-090 creates it from scratch.
- `pre-commit` is not installed in the venv — Salvatore must install it as part of ISS-090.
- `detect-secrets==1.5.0` (latest) is available via pip; `pre-commit==4.5.1` (latest) is available.
- `memory/pm.db` is in `.gitignore` (binary) — will not be scanned.
- `docker-compose.yml` contains comments referencing secrets ("private key", "password") — these are documentation strings, not credential values. Likely false positives on first scan; baseline handles them.
- Test files (`tools/tests/*.py`) reference "password" in field names/test data strings — false positives, handled by baseline.
- Pinned exact versions are a standing Dominick security condition (2026-03-14).

## Files to be touched

- `memory/workspace/shared/secrets_scanner_recommendation.md` — new file, recommendation document
- `memory/workspace/tooling/gate-output/ISS-093-G3.md` — gate output

No source code files are touched.

## G2 applicability

**G2 (failing tests) is not applicable for this ticket.** This is a pure documentation/research task — no code is written, no test can be made red before the recommendation is written. This is noted here so Alessandro can accept G2 as N/A at G5. The G2 output file will record this explicitly.

## Risks / unknowns

- **False positives in baseline**: `docker-compose.yml` comments and test file strings containing "password" / "secret" / "key" will likely appear in the initial detect-secrets scan. The baseline approach handles this: scan once, review, commit baseline, and future commits only block on *new* secrets not present in the baseline.
- **gitleaks is not ruled out for CI**: My recommendation covers *pre-commit* use only. If a full git-history scan is needed in CI, gitleaks is the better tool for that purpose. ISS-093 scope is limited to pre-commit.

## Branch created

`feature/iss-093-secrets-scanner-recommendation` branched from `main` — confirmed
